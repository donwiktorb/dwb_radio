Config = {}
-- RADIO CHANNEL MUST BE SEE BY DISCORD BOT AND HAVE TO BE SETTED UP IN SAME DISCORD AS CHANNELS 1 2 3 4 CHANNEL NAMES MUST BE 1 2 3 4... TO Config.maxChannels
Config.webhookRadio = 'CHANGEM' -- Webhook for radio
-- EVENT SAME AS RADIO BUT ANOTHER WEBHOOK AND CHANNEL
Config.DCERRORMSG = 'Cannot find your discord id. Set it by typing' -- if cannot find user discord id
Config.maxChannels = 50 -- max channels on discord server
Config.maxChannelsMSG = "The number provided is too large." -- if channel is more than config.maxchannels then too large
Config.noChannelGiven = "You need to provide a channel number." -- if no channel has been given